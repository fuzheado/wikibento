# WikiBento — Deployment Log

*Append-only. Newest first. Superseded entries here are **expected**, not wrong.*

**Why this file exists.** HANDOFF.md used to accumulate one "DEPLOYED <date> —
bundle …" bullet per session, so a document whose job was to state *what is true
now* was also a changelog: deployed bundle hashes, commit refs, dated
"verified live" notes and running test totals interleaved with present-tense
guidance. Facts written *by value* rot — by 2026-09-10 HANDOFF claimed
`main = afd308a` (actual HEAD had moved past it), "37 widget types" (the
registry held 38), and `npm test 212` (it was 229), with nothing to catch it.

The fix is the split this file makes: history belongs in a log, where a stale
entry is *normal*; HANDOFF states only the present. `scripts/docs-facts.mjs`
enforces it — the volatile-facts rules (no bare SHAs, no running test totals, no
byte-precision) apply to the present-tense docs only, and exempt this file and
`docs/ISSUES.md`, which are logs. `--live` also verifies the bundle HANDOFF
claims is deployed against what production actually serves.

Where other detail lives: **design rationale and full verification evidence** →
`docs/ISSUES.md` (ISSUE-NN per change); **feature documentation and the
"verified working" record** → `README.md`; **API behaviour and gotchas** →
`docs/DATA-SOURCES.md`, `docs/ARCHITECTURE.md`, `docs/GLAMORGAN-WIDGET.md`.

## How a deploy works

Full detail in `docs/DEPLOYMENT.md`. The shape (fresh-session safe):

```bash
npm run build        # runs the full test suite (a non-compliant widget blocks the build)
rsync -az --delete dist/ alih@dev.toolforge.org:/data/project/wikibento/www/js/dist/
ssh alih@dev.toolforge.org "sudo -niu tools.wikibento webservice --backend=kubernetes node20 restart"
```

Verify after deploying: the bundle hash in the served `index.html`, `/api/resolve`
returning 200, and `node scripts/docs-facts.mjs --live` (which compares the bundle
HANDOFF claims is live against production). `index.html` is served
`Cache-Control: no-cache` because `rsync --delete` removes the old bundles — a
browser holding a cached `index.html` would 404 (hard-refresh to clear).

## Log

Bundle hashes and commits are transcribed from what each session recorded (in
HANDOFF, or `docs/ISSUES.md` for ISSUE-numbered work); a blank commit means the
session recorded only a bundle. Status of the deployed *service* for the earliest
entries was not always re-verified — the "ships" column is what the deploy was
for, not a claim about behaviour today.

| date | bundle | commit | ships |
|---|---|---|---|
| 2026-09-24 (eleventh) | `index-DqSsco_i.js` (+ `index-BCZUdYtv.css`) | `80af3e8` | **The Wikipedia Box renders a page, and the pick brush feeds it (ISSUE-123, requested by Andrew).** The same endpoint, sanitiser and card, addressed by title instead of by `{:transclusion}`: `source: Template | Page`, a `section` of blank (the lead — 33 KB and 107 links, against 880 KB and 3,178 for the whole page), a number, a heading name, or all. A reader stylesheet does the skin's job for tables, thumbs, references and headings, and hides navbox/edit chrome. The picker needed exactly one thing: a field declaring `kind: 'page'` — the menu went from 16 to 17 types, and because an article is also a page, clicking an article link with the box armed places a card rendering that article's lead (verified 42 → 43, 'the lead · 4,972 characters · 77 links'). Also in this deploy: a link inside a box or page is a pick target at all (ISSUE-122), and a `#section` fragment no longer leaks into the value. New demo `?config=/wiki-page-demo.json`; `smoke:pick` is 22 checks. 654 tests. |\n| 2026-09-24 (tenth) | `index-g7rlMY3V.js` (+ `index-DofGHWbz.css`) | pending | **A link inside a rendered box or page is a pick target (ISSUE-122, reported by Andrew).** `WikiBoxCard` only intercepted clicks for its selection channel, so with the default `linkAction: new tab` a click in pick mode followed the link. The brush now wins: a click places a card for what the link points at, modifier-clicks still open a tab, and two rules came out of it — a `File:` link is a file on any Wikimedia wiki (the box's links are local ones) and a section link names the page, not the fragment. Verified in the built app (42 → 43 cards, no new tab) and now a check in `smoke:pick` (11). 651 tests. |\n| 2026-09-24 (ninth) | `index-CGFQXDq2.js` (+ `index-BUkYxElK.css`) | `c41a093` | **Story captions no longer show wikitext (ISSUE-121, reported by Andrew: "Sphinx, Greece, {{circa|530 BCE}}").** Captions from a gallery block are wikitext, and one `action=parse` call now renders every caption in the article in a single request; media-list's caption for the same image turned out to be truncated at the first pipe inside a link ("…[[John Wentworth (Lieutenant-Governor)|John Wentworth, lieutenant governor of New Hampshire" arrived as far as "…of New"), so the wikitext now wins where it exists; and numeric entities are decoded. The first cleaner stripped any trailing `}}`/`]]`, which broke `[[Aztec]]` and `{{circa|1434}}` and stopped them rendering — it is balance-aware now. `smoke:story` is 10/10 against production, with a new invariant: no caption may contain template syntax, an entity or a tag. |
| 2026-09-24 (eighth) | `index-Dn-0aMa4.js` (+ `index-BUkYxElK.css`) | `ee55cc1` | **The story demo board** (`?config=/story-demo.json`: a lede card plus the Met article as a story), linked from the hub and the README — the demos gate failed 8/9 while the board existed without those links, which is what it is for. Config-only: rsync, no restart. The README's story bullet also stopped pointing at the dashboard gallery (not a story) and points at this board. `npm run smoke:story` now checks the demo board as well as an imported one — 8/8 against production. |
| 2026-09-24 (seventh) | `index-Dn-0aMa4.js` (+ `index-BUkYxElK.css`) | `5525647` | **Story mode: an article's images as a continuous scroll (ISSUE-119).** The technique of the Met/Google-Arts-&-Culture prototype, ported as a DISPLAY MODE of the Gallery rather than a new widget — a story is a way of presenting an article's image list, and the repo's doctrine says a new way of *obtaining* a list is a source while a new way of *showing* one is a mode. Each panel is chosen by the image's own shape (ratio ≥ 1.55 → full-bleed; ≤ 0.8 or width < 1100 → a centred plate at native size; else a split with the alternating text side), the article's top-level sections become chapters, and there is a hero, a progress bar, lazy images and reduced motion. Verified 7/7 by `npm run smoke:story` against the built app and against production: 93 panels, mix full 10 · plate 35 · split 48 (the prototype on its own data: 8 · 37 · 46), 91 of 93 captioned, 7 chapters. Three data bugs found on the way, all swallowed by a best-effort try: the dimension lookup keyed by the API title while rows keep media-list's underscores (3 of 93 rows had dimensions), the caption join missed `{{gallery}}` templates entirely (0 of 63), and grouping by raw section ids produced 26 one-image chapters where top-level collapsing gives 7. 648 tests. |
| 2026-09-24 (sixth) | `index-Cm_RJ_t4.js` (+ `index-B4JsRcV8.css`) | `2a4cee8` | **An article for a Wiki Page brush (ISSUE-118, reported by Andrew).** `paramSources.js` defines `article` as the main namespace and `page` as the wider set, so an article IS a page — but the kind gate compared the labels and refused ("Wiki Page does not take a article"). `KIND_SUPERSET` / `kindsAccepting()` now answer what a widget accepts, one-way so a category still will not fill an article field; the refusal also reads like a sentence and names what to arm instead (`aOrAn`, `acceptedKindsLabel`). Verified 19/19 in the built app, including the reported case, and the spawned card was watched rendering the real Ada Lovelace page — the screenshot is in `docs/screenshots/`. |
| 2026-09-24 (fifth) | `index-1FZR_snL.js` (+ `index-B4JsRcV8.css`) | `0f05e7c` | **A spawned board writes only what each card reads (ISSUE-117 follow-through).** Andrew exported a board and found every spawned gallery carrying its other three sources' defaults — `page`, `category` and the default `files` list inside a gallery whose source is an article. `brushConfig` had copied every registry default. Now `minimalConfig()` strips the fields whose own `showIf` hides them (13 keys where there were 16, none dead), `+ Add Widget` starts the same way, and ⬇ Export writes through `savedBoardPayload` — so a board already on disk loses the leftovers at its next save, which is the severity model's 'inefficient… trim on write', finally applied to the write that was being looked at. The live check earned its keep in this deploy: it failed because HANDOFF still named the previous bundle. 642 tests. |
| 2026-09-24 (fourth) | `index-DBUwaOIo.js` (+ `index-B4JsRcV8.css`) | `e449892` | **Pick mode's other publishers (ISSUE-114, second pass).** Four more renderers publish: the expanded top-pages card, `RankingCard` (per row, from the row's own link — which made fileUsage, topWikipedias, topPages and cimTopWikis pickable at once), the GLAM sample filmstrip, and CIM file rows. One rule covers them: a row whose link is itself a Wikipedia article or a Commons file declares its own kind (`pickFromUrl`). The survey also shrank the list honestly — the wiki page box is an iframe, the wiki box draws bars, assessment rows are WikiProjects, the file-traffic card is a chart. SPARQL and list-source rows are held back until a row can declare its kind. Verified 18/18 in the built app, including the expanded row (3 → 4) and a ranking row's link (45 → 46); 639 tests. |
| 2026-09-24 (third) | `index-CZK2aaws.js` (+ `index-Cho8hgui.css`) | `9ca5d7a` | **Pick mode refused the second item of the same kind (ISSUE-117, reported by Andrew).** The dedupe compared every field that declares a kind, so a gallery of Ada and a gallery of Albert agreed on the three fields neither pick was about (the registry defaults for page, category and files) and the second click was refused. It is now scoped to what the card would actually show, and a pick also sets the widget's source selector — without which picking a category spawned an *article* gallery. 13/13 against the built app with the reported case as its own check; 637 tests. Also in this deploy: both browser checks now refuse to run against a stale `dist/`, which is what made the fix look broken twice. |
| 2026-09-24 (second) | `index-D_JtndJV.js` (+ `index-Cho8hgui.css`) | `88e8370` | **The pick menu reads like a menu, and the feature has pictures.** Two things a first user hit: the group headings were the singular nouns of the refusal message ("article" — right inside "does not take a Commons file", wrong as a heading over a list) and are now plurals; and the groups appeared in the order the widget names inside them happened to sort, which put Commons files above Articles, and now follow the kind vocabulary's own order. Three screenshots of the feature are in `docs/screenshots/` (menu, armed, the spawn), with the armed state verified by computed style (dashed 2px + crosshair) rather than by eye. 12/12 against production. |
| 2026-09-24 | `index-BS06xnb9.js` (+ `index-Cho8hgui.css`) | `1a42c4f` | **Pick mode: building a board out of what you are reading (ISSUE-114).** 🖌 **Pick ▾** in the header lists the 19 widget types that consume something — grouped by what they consume — and choosing one arms a brush: each click on an item inside a card places a card for that item, named in the toast, with **Undo** on the toast. It persists across clicks (the point of the feature); the same item twice refuses instead of making a twin; a kind mismatch refuses and places nothing; a spawned card targets the wiki the item's own URL names. `+ Add Widget` is untouched — a second, power-user verb beside it. Verified 12/12 in the built artefact by `npm run smoke:pick`, and against production with `node scripts/pick-mode-e2e.mjs --base https://wikibento.toolforge.org`. The first attempt at this feature was reverted for a module-init cycle; the rebuild removed the possibility (`PickMenu` imports only React and `lib/pickMode`, and receives the registry as a prop). 635 tests. |
| 2026-09-18 (eighteenth) | `index-BFulNC7j.js` (+ `index-5YMsq1dp.css`) | `fd1837e` | **`?lean=1` threw `normalizeConfigForDef is not defined` (ISSUE-113), and boards now have a stated data policy.** The import had been skipped by a guard that looked for the string `configNormalize` — which my own comment above it contained; the production bundle happened to work because it flattened module scope, which makes the failure intermittent rather than absent. Fixed explicitly, and the class got a gate: the demos sweep now loads every board in `?lean=1` and `?kiosk=1` and fails if a mode renders no cards, because a reference error can live in exactly one mode. `docs/JSON-FORMAT.md` gained the severity model for errant boards (unusable / repairable / inefficient), what is repaired silently, what is reported, and why the trim happens on write and never in a borrowed board's hands. 628 tests. |
| 2026-09-18 (seventeenth) | `index-2NqsmsxJ.js` (+ `index-5YMsq1dp.css`) | `64fdc53` | **Share links carried string booleans and every widget's defaults (ISSUE-112).** Decoding the reporter's `#/z/` link: `includeAll: 'True'`, `minSize: '200'`, `allowExternalImages: 'False'` — and `!!'False'` is `true`, so a gallery that asked to exclude caption-less images rendered as though it had included them; and each widget stored all 17 fields of the gallery definition, including the three source fields it was not using. `src/lib/configNormalize.js` coerces by the registry's field types (the Ask path had a coercion helper all along, in `deploy/server.js`, so only the server ever applied it), fills registry defaults so a compacted board behaves identically, and `compactConfig` is its inverse. Fixed at the render path (`WidgetFrame`) and in `savedBoardPayload`, which feeds the share link, localStorage and the URL fingerprint. Round-trip asserted: `normalize(compact(c)) === normalize(c)`. 628 tests. |
| 2026-09-18 (sixteenth) | `index-BLpTM1g8.js` (+ `index-5YMsq1dp.css`) | `b918e5b` | **The SPARQL default query was unusable (ISSUE-111).** A new card opened on `met-collection` — count every item in the Met's collection — measured at >75 s with the client giving up, and 41 s even narrowed to paintings only. The default is now `laureates-by-country` (2.9 s, 12 rows, a number and a bar chart), bounded twice: one anchored award item and a `LIMIT` on the group-by. Heavy presets keep their exact queries but are marked `cost: 'slow'`, appear in the ⚙ list as '— slow, may take a minute', and begin with a `# SLOW:` comment inside their own text with the narrowing to try. A test states the rule (not-slow default, a LIMIT, a warning in each slow preset), and the showcase catalog no longer selects the slow preset — very likely what has been throttling WDQS during sweeps. 625 tests. |
| 2026-09-18 (fifteenth) | `index-CgipBOCn.js` (+ `index-5YMsq1dp.css`) | `84bbdd3` | **The ⚙ regression, fixed and verified by driving the panel (ISSUE-110 follow-ups).** `fieldContext` was passed at eight call sites and never declared — a silently-failed edit — so every gear click threw `Can't find variable: fieldContext`; it is declared with `def` now. Found in the same pass: `WidgetFrame` resolved widget types with `WIDGET_TYPES[...]` rather than `widgetDef(...)`, so the ISSUE-105 compatibility rule held only in the validator and the picker — a board saved before the gallery merge would have rendered with no definition and no card at all. Browser-verified: the Board Controls Params box reads `category | buttons | Category | Images from XBio, Featured pictures, London`, `hideDecorative` renders checked, and there are no page errors. The ⚙ selector (`button[title="Configure"]`) and the scope-by-id rule are now in AGENTS.md. 624 tests. |
| 2026-09-18 (fourteenth) | `index-BdEx53B1.js` (+ `index-5YMsq1dp.css`) | `189f4a3` | **The ⚙ panel disagreed with the card (ISSUE-110).** A Board Controls card showed three buttons while its *Params* box was empty: the dashboard's `params` block is what drives the board, and nothing read it back into the box — the repo even shipped `paramSpecToText()` for the purpose. `configFieldValue` now takes board context AND honours the registry defaults, which fixed a second instance of the class (a boolean defaulting to `true` rendered checked and showed unchecked). The audit the report asked for covered five mechanisms — defaults and board params were broken, presets were already fixed, references and lookup resolution are correct by design — and the durable half is a gate that sweeps all 41 registry types for a field whose box is empty while the card renders a value. Also found and recorded: my own app build had been failing, so `dist/` was stale and one deploy shipped the previous bundle. 624 tests. |
| 2026-09-18 (thirteenth) | `index-BKqE0kUG.js` (+ `index-5YMsq1dp.css`) | `68eb227` | **Gallery thumbnails stopped paying for pixels nobody sees (ISSUE-109).** A `srcset` built from the API's own URLs (`thumburl` + `responsiveUrls`, each declared by the width it is SERVED at, since `iiurlwidth=480` reports 480 and serves 500) plus `sizes` from the grid's own column maths, and a base width per density (small → 250, medium → 330, large → 500) so small tiles stop paying for the 500px bucket. Measured on a 36-tile board, network transfer only: 1× screens 2685 KB → 1442 KB (46% less), one request per tile, 4× fewer pixels in the small slots; 2× screens now get real retina renditions instead of an upscaled 500px. Two traps measured and documented: a `srcset` whose `sizes` arrives late makes the browser assume 100vw and fetch twice (72 requests for 36 tiles), and `encodedBodySize` counts cache hits, so A/B must use `transferSize` in a fresh browser. 621 tests. |
| 2026-09-18 (twelfth) | `index-Ty6xHfJm.js` (+ `index-5YMsq1dp.css`) | `5a7fc90` | **Consulting the sister project found a live bug (ISSUE-108).** `commons-vibe`'s thumbnail benchmark documents that `thumb.wikimedia.org` serves only pre-rendered ladder widths, and that a hand-built `…/700px-` URL answers HTTP 400 — verified here (330/500/960/1280 → 200; 400/700/1000/1400 → 400). The paged viewer was zooming on `[400, 700, 1000, 1400]`, off-ladder at every step, and two tests had pinned those broken steps as expected values. `src/lib/thumbWidths.js` now owns the ladder; ceilings snap down; every step is asserted to exist. `docs/THUMBNAILS.md` records the consultation, what to adopt next (`srcset`+`sizes` from the SERVED width and `responsiveUrls`) and what not to (an IntersectionObserver — native lazy is what the sibling uses too), and `AGENTS.md` points at the local checkout. 617 tests. |
| 2026-09-18 (eleventh) | `index-CqQiCSEs.js` (+ `index-5YMsq1dp.css`) | `994e6c1` | **Gallery tiles rendered as thin bands (ISSUE-107).** A screenshot showed tiles ~600px wide and ~65px tall, each a horizontal sliver of its picture. Not a column-count problem: `width: 100%` + `aspect-ratio: 1/1` on an image inside a grid row sized `auto` is a cyclic dependency, so the row resolved against the image's intrinsic size before it loaded and never re-expanded — a 191px thumbnail in a 125px row, cut by the tile's `overflow: hidden`. A definite thumbnail height per size class (96/150/220px) plus `grid-auto-rows: min-content` removes the cycle; columns stay fluid, so a wider card gets more tiles, never thinner ones. The demos pass now fails when a tile is shorter than the image it holds — verified by injecting the old CSS back (❌ "tile 92px holding a 215px image"). 616 tests, 8/8 live. |
| 2026-09-18 (tenth) | `index-iy9GZzH5.js` (+ `index-B6skfVtC.css`) | `761c2ae` | **The Add-widget panel listed the Gallery three times (ISSUE-106).** Reported with a screenshot minutes after the gallery merge: a browser holding `gallery`, `commonsGallery` and `fileGallery` in its recents had all three resolved to one definition and nothing collapsed them. `recentWidgetDefs()` now resolves *and* de-duplicates, and the recents list is written canonically so an old list converges. The demos sweep gained a panel pass — it opens Add Widget on desktop with retired ids seeded in the recents and fails if a section repeats a widget — because this was the second fault in a day (the first: a helper called but never imported) reachable only through a surface nothing rendered. Verified by injecting the bug back. 616 tests. |
| 2026-09-18 (ninth) | `index-CHEYTt5v.js` (+ `index-B6skfVtC.css`) | `156ab48` | **The gallery family became one widget (ISSUE-105).** `commonsGallery` and `fileGallery` were confusingly named — their descriptions had to deny each other — because all three gallery widgets were named after their source. Now one `gallery` with `from: article | page | list | category`, per-source fields via `showIf` (6–10 visible, never the 17 in the definition), and the *default project belongs to the source* (en.wikipedia for an article, commons.wikimedia for a page or category). The retired ids still resolve at lookup, so saved boards and shared links keep rendering; the config infers its source from the fields an old board carries. 43 → 41 registry types, 44 → 42 catalog widgets. Verified 18/18 live across three sources and three engines. Also: a two-line registry invariant (every key is its own id — an `id:`-based deletion had silently made `commonsGallery` into `fileUsage`), and a `labelFromConfig` test for every source (the live bug that threw on every render had shipped because no test called it). 615 tests. |
| 2026-09-18 (eighth) | `index-me6J9Mg-.js` (+ `index-B6skfVtC.css`) | `bc2d2a8` | **Images from a Commons category, as a source of the file gallery (ISSUE-104).** Asked whether it should be a new widget; measured that the three existing gallery entries share four display fields and differ by exactly one source field each, so it became `from: 'list' | 'category'` on the 🗂️ Commons File Gallery — every existing board unchanged (the default is `list`), plus a tested `showIf` predicate so a source's fields appear only for that source, `newest` as a fourth order, `wiki` via the shared 364-wiki picker, `lines`/`selection` published so the card is wireable, and a subtitle that counts what the card *shows* and names the read pool when random/largest only saw part of a category. Verified live: 24 images alphabetical and 12 random from `Images from XBio` (518 files), 6/6 across engines and viewports. Also fixed: phone×Firefox in the sweep harness (Playwright cannot emulate `isMobile` there — it failed as a LAUNCH/NAV error on every demo, forever). 609 tests. |
| 2026-09-18 (seventh) | `index-ulLucS9W.js` (+ `index-B6skfVtC.css`) | `a95cb88` | **Document mode stopped re-inventing the board (ISSUE-77).** Making every card full width — the obvious way to build a "document" — reflowed the insides of cards drawn for a narrow column: a trend chart stretched to four times its width and a CIM file-spotlight image became enormous and overflowed its card into its neighbours (both reported with screenshots). Cards now take the width their **grid span** gives them (`calc((var(--print-span) / 12) * 100%)`), so the inside of a card looks like the card. The first attempt made the same mistake one level down — a flex item without a width takes its *content* width, measured at 19% of the page for one card and 100% for another. Met board Document: 21 → 11 → 8 → **7 pages**. The sweep's print pass gained a mode-agnostic invariant: every card must keep its share of the board's width between screen and print, which is the check that would have caught both reported cards. 599 tests. |
| 2026-09-18 (sixth) | `index-C1tSoMlV.js` (+ `index-D34qWrSa.css`) | `5945362` | **A print of a live board, done properly (ISSUE-77).** Three fixes from the follow-up: (1) the sheet now **waits for the board to settle** — `preparePrint` holds it until every image has decoded and no card is loading (20s cap, with a visible "Preparing the print — 42 of 139 images…" note), because `window.print()` snapshots whatever happens to be on screen and that is why the poster captured a grid of images that had not arrived; (2) Board and Document **scale to the paper instead of reflowing it** — `zoom: var(--print-zoom)` takes the board's own width to A4's content width, so a gallery's fixed tile grid or a wide table can no longer overflow a narrower print column and paint over its neighbour (the reported overlap, which was horizontal — two card boxes never touched, which is why the first check missed it); (3) Document **flows** rather than jumping, cards splitting where needed (21 → 11 → 8 pages). The sweep's print pass now waits for the settled state a real print gets and additionally flags content painting outside its own card — a print check that does not wait is a check that lies. 599 tests. |
| 2026-09-18 (fifth) | `index-CjMygZ-T.js` | `33c123e` | **A print you can choose the shape of (ISSUE-77).** The 🖨 button is a menu: **Board** (the board's own grid), **Poster** (one page, sized to the board via `@page size`, so the dialogue's scale-to-fit makes it any paper) and **Document** (one card per row, in reading order, for reading rather than recognising). The Met demo, which printed four cards on top of each other, is fixed: the sheet's first attempt grouped cards into *shelves* by overlapping vertical spans, which is right for a tidy board and wrong for a staggered mosaic — placement now comes from the layout's own `x`/`w`/`y`/`h`, so CSS grid cannot overlap two cards (13 cards, 0 overlaps, verified against production). Measured page counts: Met 11 /**1**/21 (board/poster/document), Anne Frank 6 /**1**/4. Two bugs came out of looking at the output: the poster was two pages until the page allowance was reconciled with the cards' screen heights (script cannot measure the printed layout — `beforeprint` runs in screen media), and the Met gallery printed as empty black tiles because `loading="lazy"` images below the fold had never been fetched (arming now flips them to eager: 139/139 loaded). The sweep gained a print pass — two cards touching is a failure, on every demo, in every engine. 599 tests. |
| 2026-09-18 (fourth) | `index-CKfG8oL8.js` | `d1f6c6c` | **The printed board now looks like the board (ISSUE-77).** The print sheet had been making every card full width and stacking it in DOM order, so a symmetric on-screen row (`excerpt w4 · views w2 · views w2 · excerpt w4`) became four stacked pages, two side-by-side galleries were split across pages, and the note that opens the board printed fifth. The sheet now derives its geometry from the live grid — column, span and shelf rows via `boardPrintGeometry`, so DOM order stops mattering and a URL-loaded board works the same — and lays out the same 12-column grid on paper: measured against production, the same flow as the screen and 2 pages instead of 7. Fixing it exposed two more bugs: the sheet was armed only by the 🖨 button (⌘P printed react-grid-layout's clipped transforms — there is a `beforeprint` listener now, so every print path gets the board), and a **three-second disarm timer could fire mid-print**, clearing the slots and producing a PDF with the disarmed layout — reproduced by generating the PDF in a test. The timer is gone in favour of `afterprint` + a re-arming `beforeprint`. 599 tests (a print-geometry contract, including the guarantee that a card is always on the paper). |
| 2026-09-18 (third) | `index-Dp64H275.js` | `7c8c6b6` | **A Commons-gallery widget (ISSUE-103) — Commons' curated layer as data.** A gallery is a *convention*, not a type: no namespace, no prefix (`Gallery:The Venetian Macao` is simply a missing page), just a main-namespace page with a `<gallery>` tag carrying `{{Gallery page}}` — 87,315 pages, 140,220 with the tag. The widget reads the page's **wikitext** (56 KB for London's 542 images against 654 KB for the rendered HTML, byte-identical counts) and renders the gallery's own captions and order through the existing gallery renderers, with `lines`/`selection` channels so the captions can feed a Translator and a click can drive a reader. A `commons-gallery` lookup source makes a Board Controls box a validated picker over those 87k galleries, with the `Gallery:` typo stripped because that prefix does not exist. Verified live in Chromium and iPhone WebKit: 5 captioned tiles for the Venetian Macao, **"542 images · showing 24"** for London (capped before thumbnails), and a click that loaded `File:The Venetian 05.jpg` in the reader beside it. Also added a docs gate — every demo board must be linked from the README and the hub — which immediately caught a page-picker row lost in a merge. 594 tests. |
| 2026-09-18 (second) | `index-DqGL5NOH.js` | `6d20a51` | **Wiki Stats stopped depending on a flaky upstream, and the 360° panorama renders for the first time.** The reported error (`HTTP 500 … table=wikipedias&format=csv`) was real and upstream's — reproduced for every User-Agent and only for that table, then gone minutes later, so the service is intermittent rather than dead. A single edition is now read from that wiki's own `siteinfo.statistics` (live, ~1 KB, CORS, and it returns everything the card shows: 7,241,853 articles / 1,371,008,143 edits / 263,530 active editors), while the ranking keeps the dump because nothing else lists every edition with counts. Found on the way, by the sweep's new "shows no value" check: **`PanoramaCard` had a component, a registry entry and no `case` in the content dispatcher**, so every 360° card silently rendered an empty StatCard — and the renderer gate only asserted that a renderer exists, not that it is reachable, which it now does. The sweep also gained an empty-ranking check (which caught my own near-miss on `topWikipedias`), a per-job guard after a launch failure killed a 68-run pass with no summary, and a calibrated loading-state skip. 588 tests; 67/68 against production, the one failure passing twice on re-run (WDQS throttling under the sweep's own load). |
| 2026-09-18 | `index-CLCoetYx.js` | `8ee2698` | **The pageviews card showed "—" instead of a number (found on the page-picker demo).** `getRenderer` and `transform` read an absent `displayMode` differently — "not trend ⇒ StatCard" versus "not stat ⇒ the trend payload" — so a board omitting the field got a trend payload drawn as a stat card: title, date range, no count. Every shipped board set the field explicitly, which is why only a demo relying on the default exposed it. Both now resolve the mode through one function with a test that they agree. The sweep gained the check that would have caught it — a `.stat-value` of `—` is a failure (verified by restoring the old condition: it reports exactly "shows no value: views"). Live: 166,558 views, ~5,552/day, 30 spark bars. Demo sweep 64/64 clean against production. |
| 2026-09-16 (fourteenth) | `index-Dv5Kc4UO.js` | `5e6eb8b` | **Every demo, every engine, desktop and phone (ISSUE-100).** Andrew reported an empty 📰 box on iPhone and asked whether the three-browser tests covered the demos — they loaded one board at one width. The sweep that now exists (15 boards × 3 engines × desktop + iPhone profile) found three bugs: `.widget-body`'s `flex: 1; min-height: 0` collapsed **every** phone-stack card to a 0px body (the reported symptom, reproduced in Chromium *and* WebKit, so never iOS-specific); Wikipedia serves a **navbox-stripped** parse to mobile User-Agents (22,820→5,780 bytes; `useskin`/`mobileformat` cannot change it, so the deployment relay re-fetches with the tool's own UA — 161 links restored on an iPhone profile); and a **static** widget was embedding a literal `{{widget:click-seas#selection}}` in an iframe, because ISSUE-58's unresolved-reference guard only applied to fetching widgets. Also fixed the two long-filed cosmetic bugs (✨ Ask nested inside + Add Widget; React's `key` spread into `<audio>`). Verified: **64/64 clean** against production with `--require-relay`. 581 tests. |
| 2026-09-16 (thirteenth) | `index-CCKrq_yr.js` | `ef0de8b` | **The page box learns its wiki (ISSUE-99).** A validated `lookup` param of source `article` (or the new `page`, any namespace) now takes a `project`, grows the shared 364-wiki picker beside the input, validates against *that* wiki, and commits a **reference** (`dewiki:Weddellmeer`) — so the consumers carry no project field at all. A typed prefix (`en:`, `dewiki:`, `commons:File:X`) moves the picker as you type, which makes the interpretation visible rather than silent. Measured live: `de:Weddellmeer` → the Article Excerpt, Pageviews and Quality cards all re-fetched German content, a fictional title gave ✗ *no such page on de.wikipedia*. Two revisions recorded rather than skipped: a bare `File:`/`Category:` does **not** auto-switch to Commons (a category can live anywhere, and guessing would silently reinterpret it), and the special wikis need a short-name table because the reference grammar requires `commonswiki`. Also a bug the browser found: validation was asking for a page literally named `dewiki:Weddellmeer`, so a ✗ badge sat beside German text that was plainly rendering — fixed with the tested `lookupValidationTarget()`. New demo `?config=/page-picker-demo.json`. 579 tests. |
| 2026-09-16 (twelfth) | `index-7xqRkO3-.js` | `da0add6` | **A chain that speaks in the right language (ISSUE-97's first typed value).** Four things Andrew asked for on the translate demo: the 🌐 Translator gained *Show* → original and translation / translation only / original only (defaulting to both, so nothing existing changed); the translation now travels with its language as a typed `speech` channel beside its text (`primary: 'translation'` keeps `{{widget:translate}}` meaning what it always meant); the 🔊 Speaker reads that channel through a `source` field, so the voice follows the language instead of the device default; and auto-speak now has something to say about the language it hears. Verified live with a fake voice roster: one click arms the speaker, then changing the article speaks the new French translation with `lang: fr-FR` and the French voice, no second click. Also fixed a crash the wired speaker exposed — `{{param}}` in JSX children is an object literal containing an undefined identifier (gotcha 27), hidden for months because it only renders for a speaker with no text. The project-picker gate caught a mis-generalisation: a language field has two vocabularies (a wiki, or `vocab: 'bcp47'`). 570 tests. |
| 2026-09-16 (eleventh) | `index-DEicrUCf.js` | `a02b856` | **The bare-id compatibility promise, restored (ISSUE-95/96).** `translate-demo.json` sat on *"Waiting for a reference"* because ISSUE-92 turned the Article Excerpt into a channel-mapped widget that filled only `id#extract` and `id#reference` — leaving `{{widget:excerpt-src}}`, which every board written before channels means, with nothing. A channel-mapped widget now declares `primary` and the frame also publishes that channel on the bare id; a manifest gate fails a channel-mapped widget without one. Verified live: the translate demo renders its French text. Also filed ISSUE-97, the measured answer to the structured-value question: every value today is a primitive or an array of strings, consumers infer from shape, and nothing reads the declared kind — so a typed payload on its own channel is the next step, published *beside* the text one rather than instead of it. |
| 2026-09-16 (tenth) | `index-Folo7JhJ.js` | `8aa20f3` | **Settings, and a translator that talks (ISSUE-95).** A ⚙ panel with three things and no more: *My wiki* (the ISSUE-93 picker, reused so the default and the recency list cannot disagree), *Recently used wikis* shown and clearable, and *Present mode: ask for fullscreen* — the old `FULLSCREEN_ON_PRESENT` constant as a checkbox. `translate` now emits its translation (`outputs: { kind: 'value' }`), which it never did, so a Markdown card or a Speaker can consume it. And the emitter audit is filed as ISSUE-96 with the measured table: 10 of 42 types emit, 3 declare a source picker, and the consumer side is in better shape than that suggests because any text field interpolates `{{widget:id}}`. The panel found a real bug within a minute of use — the picker recorded `jawiki` while the panel recorded `ja.wikipedia`, so the same wiki appeared twice and the ranking ignored one of them; recency keys are now canonical on write and on read. |
| 2026-09-16 (ninth) | `index-TPEFE8Mw.js` | `19eb009` | **One picker for every wiki (ISSUE-93).** 21 of 42 widget types asked which wiki to work on and five of them offered a hardcoded list (topPages 30, wikistats 13, pageviews 6, linkcount 3, categorySize 2) against a site matrix of 364 wikis and 374 languages. Now: one searchable combobox, ordered by the user's recency → their default wiki → a curated shortlist → everything else, backed by a mirrored `sitematrix` (951 projects measured), with the hardcoded constants deleted and a gate that fails the build if a new widget types out a list. The native-language-name trap was measured the hard way: the matrix returns 中文, so 'chinese' matched nothing until every option carried both names. Verified live: the served manifest declares the picker for both a project field and a language field. |
| 2026-09-16 (eighth) | `index-Ckn8aPWg.js` | `ce18129` | **Every page carries its wiki, and prose never travels alone (ISSUE-92).** The Article Excerpt now publishes two channels — its paragraph on `extract` and the page it came from on `reference` (`enwiki:Albert Einstein`) — and fifteen page-taking widgets accept a reference through one helper instead of fifteen hand-rolled parsings. A manifest-constitution gate refuses a prose emitter that declares no reference channel, so the context cannot be dropped again. The sweep found a boundary bug worth remembering: the resolver returned the canonical dbname while the app's fetchers build `https://${project}.org` from the dotted form, so fifteen widgets produced `https://enwiki.org` at once — a DNS failure that looks like nothing in particular, caught only by driving the demo. Both names now travel: `project` for the wire, `projectConfig` for fetching. Verified live: production's manifest declares both channels and the translate demo renders. |
| 2026-09-16 (seventh) | `index-BpK0Pnqh.js` | `f8ccba1` | **The Article Excerpt, named and placed honestly (ISSUE-94).** Its registry description now names the source and the API (*the REST summary of a Wikipedia article — every language*); the type id stays `excerpt` because it is the config contract every board and guide depends on, and the registry's own pattern already answers the generic-vs-specific question (generic names for generic functions, specific names for specific sources). Layout: the frame now honours `config.verticalAlign` (top | center) — a general option, unchanged for any card that declares nothing — and the excerpt defaults to top, so a short paragraph hangs from the top of a tall card instead of floating in the middle. Measured 139px/139px before, 12px/524px after; the shipped translate-demo board verified live at 12px above, nothing clipped. |
| 2026-09-16 (sixth) | `index-DTO6rAQ8.js` | `a92ca69` | **Pages travel with their wiki (ISSUE-92), and the wiring guide exists.** A published page is now a *reference* — `enwiki:Weddell Sea` — using the dbname every Wikimedia API already speaks; a bare title still parses as a title, so nothing existing changed meaning. A Wikipedia box's `selection` publishes one and `wikiPage` consumes one (the value's project beats the consumer's own config). The convention immediately caught a bug it did not create: `wikiPage` built `https://${project}.org` by hand and produced `https://enwiki.org` for the first reference it saw — it now uses the shared `projectSite`. Documentation: `docs/WIRING-BOARDS.md` (the advanced guide: sources, consumers, channels, references, what cannot be wired) plus a bridge section in the GUIDE, so the vocabulary is stated once instead of drifting. The language-picker epic is filed as ISSUE-93 with the measured numbers (a 6-option select covers 1.6% of the 364 wikis). |
| 2026-09-16 (fifth) | `index-CF0eBcoA.js` | `d3fec56` | **Click-through from a rendered box (ISSUE-91).** Named output channels: a widget may publish more than one thing (`outputs: { items: 'lines', selection: 'value' }`, stored as `id#channel`, resolved as `{{widget:id#selection}}`, offered in the source picker, carried through renames) while the default output keeps the bare id, so nothing existing changed meaning. `wikiBox` publishes what the reader clicked — `/wiki/Weddell_Sea` becomes `Weddell Sea` — and ⚙ *Links in the box* chooses new tab / send to the board / both. Verified in production: clicking a sea in a live `{{List of seas}}` card (161 links) loaded the article in the page viewer beside it and put the string in a Value Display card. Three gates enforce the new shape; a reference to an undeclared channel now fails the demos constitution. |
| 2026-09-16 (fourth) | `index-68WWsiDw.js` | `f12a22c` | **A click in a box opens a new tab (ISSUE-91).** `wikiBox` rendered MediaWiki's own anchors, which have no `target` — sensible on the wiki, destructive here: clicking "Weddell Sea" in `{{List of seas}}` replaced the whole board. Anchors are now rewritten to `target="_blank" rel="noopener noreferrer"` **before** the sanitiser (so the attribute survives the allowlist), matching the 36 other content-link sites in the app; an anchor asking for another target is respected. Verified live: 111 links in the five demo cards, all 111 targeted, a click opens a new tab and the board keeps all five cards. The richer pattern — a click that *means something to the board* (load a page into another widget, emit the title to a map or gallery) — is specced in ISSUE-91 with an audit of what is possible today: nothing emits because a reader clicked, and a cross-origin iframe never reports its clicks. |
| 2026-09-16 (third) | `index-CQrYr5Qg.js` (+ `index-BWgKdWiq.css`) | `1cd545a` | **📰 Wikipedia boxes (ISSUE-90).** A new widget renders any Wikipedia template with the wiki's own markup *and* its own TemplateStyles: `action=parse&text={{In the news}}` returns both in one CORS call, because parsing a *transclusion* skips `<noinclude>` (the documentation furniture) and TemplateStyles arrive inline and already scoped to `.mw-parser-output`. Five cards ship as a demo (`/front-page-demo.json`): In the news, Today's featured article, Did you know, the selected anniversaries and the real Picture of the day. Three traps, all now documented: wrapper templates only render in the Main Page context (the dated subpages return the real boxes), `{{CURRENTYEAR}}` magic words collide with board params (hence the widget's own `{date}` tokens), and the API returns relative URLs that would otherwise send readers to wikibento. Untrusted input goes through an allowlist sanitiser and a CSS rule filter; the box also emits one line per item so it can drive a dataflow board. Verified against production: all five boxes render, 0 relative URLs, nothing clipped. |
| 2026-09-16 (second) | `index-DuKM6csr.js` (+ `index-CI1Ga_V5.css`, unchanged) | `9de72cc` | **A big board now fits in a QR code (ISSUE-89).** The self-contained share link is now gzipped before it is embedded (`#/z/…`, with `#/d/…` still loading forever), which takes the boards in `public/` from 1 of 15 fitting a QR to 13 of 15 — the reported 4,012-character board becomes 900 characters and renders a QR. `buildCompactShareLink` picks whichever form is shorter, the two boards that still cannot fit are refused as before, and the failure message now offers the transfers with no length limit (copy the link, or Export → AirDrop → Import) instead of telling the user to trim their board. Cost one trap, now gotcha 23: a `CompressionStream` deadlocks if the writer is closed before the readable is read, silently falling back to the long link — Node does not reproduce it, and the audit caught it. Verified against production: 18 actions traced, 0 invariants broken. |
| 2026-09-16 | `index-BDLuwt1c.js` (+ `index-CI1Ga_V5.css`, unchanged) | `b44cc2a` | **A shared link borrows a board (ISSUE-88).** Opening someone's `?config=` link no longer overwrites the visitor's saved board: a URL board is *borrowed* — shown, never written — until the visitor edits, at which point it is adopted and the board it displaced is kept recoverable for a day. A slim notice appears only when something is at stake: *"👀 Viewing a shared board — GLAM. Your own board is saved and untouched"* with [Save this as mine] / [Back to my board], becoming *"💾 Your previous board is saved — recoverable for today"* with [Restore my board] after an adoption. One signal does both jobs: the fingerprint divergence that drops the URL's claim is the moment of adoption. Verified against production by the audit: 17 actions traced, 0 invariants broken. |
| 2026-09-15 (seventh) | `index-DnrNeY2o.js` (+ `index-CI1Ga_V5.css`, unchanged) | `18ea65b` | **ISSUE-87, corrected by its own audit.** Asked to confirm the fix was complete, re-auditing found four defects: a unit test that could never fail (it compared a value with the table it is derived from), inventory gaps (link navigation — the one place a push is right — plus the speaker, diagnostics and undo-does-not-re-claim), the suite list and GUIDE not mentioning the new behaviour, and mount-time layout placement being counted as an edit, which killed the claim on arrival for any config whose layout had gaps. The gesture that drops the claim is now drag/resize *start*: RGL 2.2 fires a *stop* handler while placing the board. The audit traces one edit per class — widget removed, param changed, card dragged — and was run against production after this deploy: 11 actions, 0 invariants broken. |
| 2026-09-15 (sixth) | `index-CXkEamrO.js` (+ `index-CI1Ga_V5.css`, unchanged) | `4820f2d` | **The URL stops lying (ISSUE-87).** ↺ Reset dropped its `?config=` claim, and so does every board edit — an effect on the board fingerprint, so the address bar can no longer describe a board that is no longer there. Share builds its link from the board on screen (#/d/<payload> once the config claim is stale) instead of echoing the address bar, which previously handed recipients the file's board rather than the one being pointed at. All URL access now goes through `src/lib/urlState.js` (one reader, one writer, enforced by a source scan), and `npm run smoke:url` traces 9 actions in Chromium — verified in production, 12/12 checks, plus a 3s guard that a quiet load is not an edit. |
| 2026-09-15 (fifth) | `index-BEaAHn9L.js` *(unchanged)* | `b3532a6` | **Config-only: Andrew's own tighter layout for `/document-reader-demo.json`.** He edited the board himself (height 39 → 34 by resizing five cells, plus the app's export fields `moved`/`static` and a `params: null`), asked for it to be noted and deployed, and nothing else changed — the JS bundle is byte-identical, so no restart was needed and the served JSON was diffed against the repo copy to confirm it: **identical, 4,453 bytes**. The board renders live with five cards, the transcription panel open on load and all four documents reporting 329/96/38/16 pages. The only byte I touched was a missing trailing newline. |
| 2026-09-15 (fourth) | `index-BEaAHn9L.js` (+ `index-CI1Ga_V5.css`) | `f0ae35a` | **The transcription panel becomes a sticky toggle.** Two notes from Andrew, which were one fix: the Wikisource text should be visible when the card loads, and it should not vanish on a page turn (he had to press ¶ again each time). Now the panel opens by default when its source has text and the card asks for it (⚙ *Transcription* on the 📄 Document Reader — "show it from the start" or "hidden until I press ¶"), it **follows the reader**: turning a page reloads the text for the page in view, with a sequence guard so a slow response cannot overwrite a newer one, and ¶ is a genuine toggle with `aria-pressed`. A panel that stayed open showing the *previous* page's words would be worse than one that closed, which is why the reload is part of the same feature. The IA Book card keeps its ¶ opt-in — there the text is a bonus, not the point, and the panel costs the page its room — but it is sticky too. Verified live: showing on load with ¶ already pressed, no panel on untranscribed cards, page 19 updating itself to 1,458 characters under EN.WIKISOURCE · VALIDATED, page 20 keeping it open with 1,598 different characters, ¶ closing and reopening — **8/8**, no uncaught JS errors. `smoke:document` 37 assertions, `smoke:iabook` 33 (its test now waits for the words rather than for the container, which the change exposed as a race). 439 tests. |
| 2026-09-15 (third) | `index-B8NGcYXL.js` (+ `index-CI1Ga_V5.css`) | `a1c48a7` | **The Wikisource text layer.** Where volunteers have transcribed a Commons document page by page on **Wikisource**, the 📄 Document Reader grows a **¶ button** showing each page's text **with its proofreading grade** and a link to the transcription. One `globalusage` call detects a transcription (a `Page:`/`Index:` usage on a Wikisource — a cross-wiki *link* is not one); **one** `prop=proofread|revisions` call per page returns the words *and* the grade, which is the point: a merely scanned volume says *"Not proofread (uncorrected OCR)"*, a validated one says *"Validated"*, and a reader has to know which they are quoting. The demo fixture is a 38-page, 0.89 MB validated pamphlet (`"Homo Sum"`); the 1,208-page bulk-OCR reference set the traps were measured on is kept for measurements, not for every run. Also here: the demo's cards are named after their works (`dr-homosum`, `dr-mozart`) because a capability is not a widget. Verified live: five cards reporting 329/96/38/16 pages, ¶ on the transcribed card and absent on the untranscribed ones, page 19's 1,458 characters under EN.WIKISOURCE · VALIDATED, the 42-card catalog with no crash, the IA and 1929 boards unchanged, both endpoints 200 — **17/17**, no uncaught JS errors. 439 tests, `smoke` 252 measurements, `smoke:document` 31, `smoke:iabook` 33. |
| 2026-09-15 (later) | `index-YJ87Jsl0.js` (+ `index-CIDXj2nC.css`) | `c61042c` | **The Document Reader** — read a **PDF or DjVu from Wikimedia Commons** (or any wiki) page by page in the *same* viewer the Internet Archive books use: turn, zoom, jump to a page, **facing pages**. One `imageinfo` call gives the page count and a page-1 render template, so a 329-page book costs what a 2-page one does; the credit comes from `extmetadata`, and **Open the original** hands the file to the browser's own PDF viewer. Two additions to the shared reader came out of it: `caps.maxWidth` (a source's ceiling) and `caps.widths` (a source that serves a fixed width list — see the trap below), plus a **page-jump** control, which a 329-page document needs, and a "this page did not load — open the original" sentence instead of a blank hole. New demo board `/document-reader-demo.json` (a 329-page PDF + a DjVu beside an IA book: one reader, two archives). **The trap, found only by running it:** a document page render is served at only certain widths — 120/250/330/500/960/1280 work, while 70/150/200/320/400/640/700/800/1024/1200 return an HTTP 400 **HTML** page, which Chrome then refuses to give an `<img>` at all (`net::ERR_BLOCKED_BY_ORB`) — so the first run showed blank cards with no icon and no message. Identical on PDF and DjVu, and *not* MediaWiki's image-thumb set. Verified live: the document board's three cards report 329/96/16 pages and the DjVu reaches page 96 of 96; the showcase catalog renders **42 cards** including the new one, with no crash; the IA board still opens as facing pages; the 1929 board still draws 21 timeline dots; `/api/resolve` and `/api/proxy` 200; front door fine — **14/14**, no uncaught JS errors. 421 tests, `smoke` 42/42 widgets and 252 panel measurements, `smoke:document` 24, `smoke:iabook` 33. |
| 2026-09-15 | `index-D9YNjKJA.js` (+ `index-DGfyoQMG.css`) | `8220b99` | **The Internet Archive pass, and a shared page reader.** 📖 **IA Book** — a scanned archive.org book read in a new shared viewer: turn, zoom, a thumbnail strip, **search inside** (IIIF Content Search, each hit naming its page and showing the matched word boxed on it), per-page OCR text, and PDF/EPUB/OCR/DjVu links. The viewer (`src/lib/pagedViewer.js` + `src/widgets/PagedViewer.jsx`) reads a *page source*, so a Commons PDF/DjVu reader (ISSUE-82) reuses it — and **facing pages** with **right-to-left** support (Arabic/Hebrew/Yiddish scans put the later leaf on the left) landed once for both, settable per board via ⚙ *Reading mode*. 🎬 the media player now takes **direct media URLs** — an `archive.org/download/…` file plays with no API call and no key (Range requests make seeking work); the codec trap recorded: Chromium plays mp4/H.264 and reports `canPlayType: ""` for Theora `.ogv`. ⤓ **PNG export for CORS-image widgets** (ISSUE-80): an HTML/CSS card whose image host allows CORS rasterises safely, so a book page exports as a real PNG (4.4 MB at 2×). Plus **two demo boards** — `/internet-archive-demo.json` (two books in facing-pages mode, four archive items, two players) and `/anne-frank-mlk-demo.json` (one story: two lives on one axis with prose, images and traffic). Verified live: the showcase catalog's **41 cards with zero crashes**, both books showing facing pairs with both page images loaded, the film player at `duration 664s`, 21 timeline dots on the 1929 board, the older Lifeline board still rendering, `/api/resolve` 200 and `/api/proxy` relaying 200 — **10/10**, no uncaught JS errors. 403 tests, docs-facts 7/7, `--live` 8/8. |
| 2026-09-14 | `index-CSKC-SIk.js` | `1e54db7` | **The Lifeline catch-up deploy — 28 commits.** Two timelines on one shared axis (`timeline` renderer + `two-lives` and `curie-pair` presets, calendar *or* age alignment, − / + zoom to 8×, an overlap-band toggle, a light card theme, per-card titles); a **⤓ export menu** (PDF, CSV, PNG, SVG) per widget; the **`mul` label fix** (Marie Curie and every other language-neutral name were rendering as `Q7186` — this affected *every* entity-labelling widget, not just the timelines); the **preset query** now visible in the ⚙ panel instead of an empty box; and **`BarCard` restored** — rewriting the timeline card in `e4cdad8` had deleted it, so bar-rendered SPARQL widgets threw `ReferenceError` on main for three commits (never in production, which predated that commit). Verified after deploy: 46 timeline dots across 4 lanes, 40/40 showcase widgets with zero crashes, 40 export menus, PNG correctly disabled for HTML/CSS widgets, `/api/resolve` and `/api/proxy` both 200. |
| 2026-09-12 | `index-C8b1z4iU.js` | `8334f74` | **Widget names, for the video pipeline.** Every `.grid-item` now carries `data-widget-id`, so a recorder (or a script, or a test) can address a card by name instead of by position — `[data-widget-id="views"]`, never `:nth-child(3)`. That is what made the [tutorial video](TUTORIAL-VIDEO-STATUS.md) scriptable. *Reconstructed 2026-09-14: this deploy was never logged at the time. The commit is the one whose features production has, not necessarily the exact build that produced the bundle — and it is the last deploy recorded here, so everything after it is committed but not live.* |
| 2026-09-12 | `index-DkeLIrq2.js` | `d176f79` | **Vocabulary: everything is a widget.** The product copy called the same box a “card” in places and a “widget” in others, and the tutorial video shows both the Reset dialog and the Add Widget picker on camera, so the two disagreed on screen. Fixed: the Reset dialog’s message (“every widget”, “the three-widget starter set”), the Markdown widget’s own text, the registry description for it, the config panel’s “Params on this widget” plus its hint, the “none of this widget’s selected params” notice, and the QR density warning. Left alone deliberately: `Stat Card`, which names a *display mode* (against `Trend Chart`), not the box. Two deploys minutes apart—`index-DJd2USKl.js` carried the main sweep, this one the remaining hyphenated “three-card” a scan had missed |
| 2026-09-11 | `index-gl3ILPmw.js` | `df06d4f` | **Reset now asks, and params travel.** Three product bugs the tutorial could not honestly teach around: Reset opens a dialog (Cancel · **Blank board** · **Starter set**) and clears the board's `params` block with the cards; **Export and 🔗 Share both carry `params`** (both silently dropped it, so a parameterised board lost its controls through Export → wiki page → `?config=` and through a shared link); and `persist()` at five call sites wrote `params: null` when only widgets/layout changed, erasing the block from localStorage as soon as a card was moved. New `src/lib/savedBoard.js` also fixes boot: an **empty board is now restorable** (the old rule required non-empty widget and layout arrays, so a deliberately blank board came back as the three starter cards). Verified in a real browser — 22 checks: the dialog's three choices, blank-board survival through a reload, export payload, params surviving a drag and the reload after it, and a share-link recipient receiving both widgets and params. Needs no `deploy/server.js` change |
| 2026-09-11 | `index-DYsqgVGH.js` | `9627585` | Second deploy: the Commons Impact Metrics **allow list now ships with the app** (`public/cim-allow-list.json`, 1,775 categories / 78 KB), so the lookup's instant suggestions no longer depend on the `/api/proxy` relay — verified in the browser that the live page reads `cim-allow-list.json` and never calls the relay. Also ships `src/lib/cimAllowList.js` and the reworked `src/lib/paramSources.js` loader (bundled → relay → direct). `deploy/server.js` unchanged |
| 2026-09-11 | `index-B-t2hIBH.js` | `1167e25` | **The big catch-up deploy** — 35 commits: 🔳 IA Item widget (39th type, `iaItem`), share lean/full mode, Wayback tile states, the validated `lookup` param (ISSUE-68) with its allow-list relay, the CIM registration correction, and `deploy/server.js` (+156/−74 — pushed separately; the standard `dist/` rsync alone would not have carried it) |
| 2026-09-10 | `index-6udjc6im.js` *(unchanged)* | `b0ef076` | Config-only deploy, third that day: the glam demo's 🏆 CIM Global Leaderboard switches to `scope: shallow` (direct category attribution instead of the diffusion-inflated tree rollup — deep and shallow share only 39 of their top 100), the demo hub's markdown count is corrected to 38 widget types, and the README's demo table to five institutions |
| 2026-09-10 | `index-6udjc6im.js` | `e658dab` | Second deploy: finally ships the 🔳 **QR widget** (PR #47 — merged earlier the same day, never deployed) together with the new `deploy/server.js` (the Ask dataflow manual is now *derived from the manifest*, so a new emitter can't be silently missing from the prompt), the complete **39-widget showcase catalog** (🛒 Board Controls driving an `article` param that re-aims five cards, 🔳 QR), the `docs-facts` constitution and the HANDOFF now-document split |
| 2026-09-10 | `index-D9wl_Ty8.js` | `534cff7` | ISSUE-64: TrendCard Y-axis ticks + gridlines + `zeroY` scale toggle (Article Pageviews trend, CIM Views Over Time) — GitHub #42, PR #43 |
| 2026-09-10 | `index-TTSz7Lkm.js` | `edd68f0` | ISSUE-44 Phase 3a: Ask **board assembly** (describe a board → wired params + widgets + `{{widget:id}}`, added below the current board with Undo). Superseded same day by the ISSUE-64 deploy |
| 2026-09-09 | `index-DxO6r8uA.js` | `e29fe77`, `e75dd19` | Demo suite + `?config=/demos.json` hub (ISSUE-63), custom-URL embeds (ISSUE-62), rate-limit guards (ISSUE-61), user guide + config-URL error handling (ISSUE-60), Ask manifest v3 |
| 2026-09-09 | `index-BWKLfppo.js` | — | First six-PR session deploy: 🔊 Speaker (#17), 🌐 Translator/MinT (#21), request-serial guard (#24), ⚙/ⓘ panel reachability (ISSUE-54, #31), docs research series (#30), ROADMAP Phase 2.5 (#29) |
| 2026-09-08 (recorded) | `index-DWKLfppo.js`, `index-CGBDkEU8.js` | — | GLAM view-budget + ISSUE-53 (widget instance names / rename repointing). `DWKLfppo` as recorded — likely a typo for `BWKLfppo` |
| 2026-09-08 | `index-OJOY0xwd.js` | `e4d248b` | ISSUE-52: widget-to-widget dataflow (`source` picker + `{{widget:id}}` + the 🧾🔎🔢🖨️ chain) |
| 2026-09-08 | `index-DHc3p4sT.js`, `index-BLGokffr.js` | `481dee2` | GLAM 2026-09-08 work (recorded as a group; `BLGokffr` = full merged main incl. the view-budget fix) |
| 2026-09-08 | `index-ejrRtwiS.js` | `22375cc` | GLAM view-budget 150 → 2,000 pages, 429s retried as transient, `· N pages failed` surfaced |
| 2026-09-08 | — | `3f219d9` | Firefox/Safari CORS fix (no `User-Agent` header on browser GETs — it triggered preflights) + the cross-browser matrix; also ISSUE-51 gallery options (#12) and QID→label resolution (#11) |
| 2026-09-03 | `index-BgEdNEa0.js` | `3f219d9` | Cross-browser fix land + `npm run test:browsers` |
| 2026-09-03 | `index-DClvfKWq.js` | `ba33105` | CIM shallow-vs-deep gap indicator (Issue #5) |
| 2026-09-01 | `index-BHGlMTfE.js` | — | CIM month-lag fix: `latestCimMonth()` resolves the latest *published* month; 4xx fetches terminal |
| 2026-09-01 | `index-D5iaTtCr.js` | — | CIM File Spotlight image preview |
| 2026-09-01 | `index-DBwecE6U.js` | `5d941c8` | Media player: Commons description + `Artist · License` credit, Markdown annotation |
| 2026-09-01 | `index-DjTAjvPz.js` | `d1f3860`, `b584f60`, `00b2dd7` | ISSUE-50 board params prototype: `{{param}}` interpolation + 🎛️ Board Controls, then number slider + month stepper |
| 2026-08-17 | `index-B_hgqo4i.js` | `ebb4af7` | GLAM PetScan relay (ISSUE-46) + `fileBudget` ceiling 1,000 → 30,000; ISSUE-47 clickable GLAM/CIM titles |
| 2026-08-16 | — | `ee70ce4` | Gallery full-width defaults + content-fit height; found `gridConfig` prop drift (rows were rendering at RGL's 150px defaults) |
| 2026-08-16 | `index-DkcrAAk0.js` | `3bfad47` | ▣ Lean mode; prior same-day `index-DdJRNUuD.js` (`c9f7bbc`) = 🎬 Video/Media Player (ISSUE-39) |
| 2026-08-16 | — | `5378088`, `165c014` | ✨ Ask advisor (ISSUE-44 Phase 1) + `/api/ask` LiftWing relay |
| 2026-08-15 | — | `3c94ab8` | Kiosk mode (⛶ Present, `?kiosk=1`) — ISSUE-18 |
| 2026-08-14 | `index-C3DXQFad.js` | `a7f5ee7` | Freshness constitution (⏱ last-run stamp on every fetch widget) |
| 2026-08-13 | `index-CyCFd4ac.js` | — | Grid drag fix (`dragConfig` handle — widget drags were starting anywhere) |
| 2026-08-13 | `index-D4DEEPkT.js` | `68dea21` | 🗂️ Commons File Gallery + 📋 Article List (pasted-list inputs) |
| 2026-08-13 | `index-DiStWjwF.js` | `7e022f7` | The 8 CIM widgets (Commons Impact Metrics) |
| 2026-08-13 | `index-BJjaG_ta.js` | `bfbce6e` | 🧠 SPARQL Query widget (WDQS/QLever/Humaniki + presets) |
| 2026-08-13 | `index-BdMTw21V.js` | `f7dfa44` | 📄 Wiki Page iframe embed + mobile view |
| 2026-08-13 | `index-bbwxWEKh.js` | `4cf2c93` | Gallery square tiles + letterboxing (`imageFit`) |
| 2026-08-13 | `index-BqgxhKa5.js` | `4cf2c93` | Gallery overflow fix (content-height card covered its own header) |
| 2026-08-13 | `index-BEguwTL7.js` | `4cf2c93` | 🖼️ Article Gallery (REST media-list + batched imageinfo) |
| 2026-08-13 | `index-BSwIQuK-.js` | `ec9d26c` | 🌐 360° Panorama Viewer (vendored Pannellum, lazy asset) |
| 2026-08-13 | `index-CF9Vo_m5.js` | `b9d62f7` | Article Vitals: 📄 Excerpt, 🕓 Edit History, 🏅 ORES Quality, 🧭 WikiProject Assessment |
| 2026-08-12 | — | — | First Toolforge deploy: `node20` webservice serving `dist/`, plus `/api/proxy`, `/api/resolve` |

## Repository housekeeping

### Stale remote branches deleted (2026-09-11)

Four remote branches sat on GitHub long after their content had landed on `main`
(they were listed as unmerged only because the *files* arrived by another route).
Each was re-checked against `main` before deletion, and the record is kept here so
the old wording survives the branch:

| branch | tip | what it held | why it could go |
|---|---|---|---|
| `docs-tapestry-eval` | `8d93eb9` (2026-09-08) | `docs/TAPESTRY-EVALUATION.md`, 233 lines | **byte-identical** to `main`'s copy |
| `docs-research-landscape` | `7cc4637` (2026-09-08) | the same file, 233 lines | **byte-identical** to `main`'s copy |
| `docs-demo-ideas` | `8f813f3` (2026-09-08) | `docs/DEMO-IDEAS.md`, 209 lines | `main`'s copy is the expanded one (403 lines; +198/−4) — a superseded draft |
| `docs-issue-65` | `becf32f` (2026-09-10) | the `ISSUE-65` entry in `docs/ISSUES.md` | `main` carries the same entry marked **done (branch `feature-qr-widget`)** with its progress notes; the branch still said **open** |

The only text that existed solely on a branch was four draft lines in
`docs-demo-ideas`, kept here for completeness (main's Voyager catalog replaces them):

```
(prioritized plan), `MODULARITY-AND-DATAFLOW.md` (params + dataflow), and
`docs/AGENT-MEMO.md` (conventions). Each entry lists what's on the board,
- Voyager Company (Wikipedia): A Hard Day's Night · The Complete Maus · Who
  Built America? · Criterion Goes to the Movies, etc.
```

Deleted with `git push origin --delete`; nothing else was unique to them, and the
content above is the whole delta.

## Notable deploy-time findings

The lessons that only showed up *after* deploying — worth keeping because each
one changed the code or the procedure. Nothing here is a current to-do; it is
the record of what deploying taught us.

- **A deploy can be real while the bundle hash stays the same** (2026-09-10). The
  third deploy shipped only `public/*.json` changes (a board's widget config, a
  markdown count) — Vite copies `public/` verbatim instead of bundling it, so the
  JS hash was identical before and after. Verifying "the bundle hash changed"
  would have proved nothing; the serve-side check has to read the shipped JSON
  (`/glam-demo.json`, `/demos.json`) and, best of all, the rendered result. For
  the leaderboard scope change the decisive check was comparing the live card's
  rows against the shallow API response for the resolved month — the card read
  `1. Uploaded with VicuñaUploader 4,978,286,576`, matching shallow, where deep
  would have shown UNESCO 6.1B.
- **`dist/` is not the whole deploy — check `deploy/` too** (2026-09-10). The
  second deploy needed `deploy/server.js` as well as `dist/`, because the Ask
  dataflow manual had moved from a hardcoded list to one derived from the
  manifest. Shipping only the new `dist/` would have left production running the
  old server that still claimed "only these five widgets emit" — wrong for the
  first time a sixth emitter (qrCode) existed. Before deploying, diff the
  deployed commit against `HEAD` and include `deploy/` when it changed:
  `git diff --stat <deployed-commit>..HEAD -- deploy/`.
- **A deploy can carry several sessions' merged work.** This one shipped the QR
  widget (PR #47), the derived-manual server change, and the catalog/docs work
  together, because `main` had accumulated merged-but-undeployed commits. Verify
  the *live* result, not just the deploy commands — the checklist used here was:
  served bundle hash, `/api/resolve` → 200, `/manifest.json` (v3, `widgetCount`,
  emitters present), `/dashboard.json` (39 widgets / 38 types / `params.article`),
  and a browser pass on the live board (frames rendered, 0 error frames, the QR
  card's inline SVG, and the param switcher re-aiming all five cards).
- **`main` SHA and running test totals do not belong in prose** (2026-09-10).
  HANDOFF's `main = afd308a` was already stale, and its `npm test 212` was 229 by
  the time anyone read it. Now enforced by `scripts/docs-facts.mjs`.
- **Don't quote build bytes — and never diagnose an artifact diff without
  pinning the commit first** (2026-09-10). Exact byte counts change with every
  source change, so README quotes a *magnitude* and `docs-facts` bound-checks it
  instead of pinning decimals. The related trap cost a **wrong diagnosis** the
  same day: a rebuild of the working tree did not match the deployed bundle,
  which invited a "toolchain drift" explanation — but `HEAD` had moved past the
  **deployed commit**, and the QR widget (PR #47) had never been deployed.
  Rebuilding the deployed commit `534cff7` with today's toolchain reproduced the
  live bundle **byte-for-byte** (md5 `3482ffbb752e6c097f04d7d3fcf6ca27`), so
  nothing had drifted. (`react-grid-layout` *is* pinned exactly, after two silent
  API-drift incidents — `dragConfig` and `gridConfig`, see
  `docs/ARCHITECTURE.md` — but that is a separate, real lesson.)
- **Merged is not the same as live.** Production ran `index-D9wl_Ty8.js` (built
  from `534cff7`) while `main` had already merged the 🔳 QR widget (PR #47): the
  live bundle contains none of its code. A "current production bundle" line says
  *which* artifact is live, never *how current* it is — check the gap with
  `git log --oneline <deployed-commit>..origin/main`.
- **A view-budget fix can still undercount through 429s** (2026-09-08).
  Verifying the GLAM view-budget fix live exposed a *second* silent-wrongness bug:
  the view walk's ~285-request burst trips the pageview API's rate limiter, and
  429s were terminal in `fetchTextWithRetry` **and** zero-filled by
  `fetchMonthlyViews`' catch-all — one live run silently lost ~65% of views while
  a clean run matched the offline reproduction exactly (1,375,031). Fix: 429 is
  transient (404 stays terminal), `fetchMonthlyViews` returns `null` on
  non-404 failure (0 keeps meaning "genuinely no data"), and the card appends
  `· N pages failed`. Lesson: a partial result must be *quantified*, never zeroed.
- **CIM's 404 is ambiguous** (2026-09-01). A registered category with no data for
  the requested month returns the same "not loaded yet" body as an unregistered
  one — and because the disambiguation probe was built from the same
  `prevCimMonth()` as the main request when `month=0`, probe ≡ main → both 404 →
  a false "register via {{Views from category}}" verdict for long-registered
  categories at every month start. Fixed with `latestCimMonth()`.
- **Wikimedia rejects a `User-Agent` request header from browsers** (2026-09-03).
  Chromium strips it before the CORS preflight check (which masked the bug);
  Firefox and WebKit preflight *with* it, and RESTBase's allow-list accepts only
  `api-user-agent` while CIM 405s `OPTIONS` — producing the exact working/failing
  split seen on iOS Safari. Full diagnosis:
  `docs/BUG-REPORT-ios-safari-fetch.md`. Never set custom headers on browser GETs.
- **`rsync --delete` plus a cached `index.html` 404s the app** (2026-08-12 →
  fixed). Old bundles are removed on deploy, so a browser holding a cached
  `index.html` requests a bundle that no longer exists. `index.html` is served
  `Cache-Control: no-cache` (assets stay immutable).
- **Measuring again beats trusting the claim — twice.** The `gridConfig` drift
  (2026-08-16) was invisible because the board *looked* fine at RGL's 150px row
  defaults, and the panel-clipping bug (ISSUE-54) was invisible because the
  catalog's authored sizes mostly pass while 21/35 widget types clip at the
  fresh-add size. Both became constitutions (`npm run smoke`,
  `npm run smoke:panels`) rather than one-off fixes.
